[build]
  command = "cd site && npm install && npm run build"
  publish = "site/dist"

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm --prefix site run dev"
  targetPort = 4321
  framework = "#custom"
  autoLaunch = false
