# Weekly Heartbeat — Week of 2026-09-20

## Board Movement

- **Completed (Now → Done):**
  - PR #140 - prior weekly heartbeat (week of 2026-09-13). Opened 09-13 12:05 UTC, merged 09-13 20:27 UTC, after that heartbeat's own snapshot was taken. Merged with one review correction: the hygiene pass's age was restated as one elapsed week rather than two, since two heartbeat appearances span one week.

- **In Progress (Now):**
  - PR #112 (the-alignment-system) - resumed 09-18 after sitting content-idle since early June. Five content commits in the window; module reset to `v1.0.0` frontmatter (from `v1.1.0`). Marked ready for review 09-18; no reader feedback arrived — the author resumed unprompted.
  - PR #114 (the-case-for-self-work-as-service) - resumed 09-18 after sitting content-idle since early June. One content commit in the window; module reset to `v1.0.0` frontmatter (from `v1.1.1`). Marked ready for review 09-19; also resumed unprompted without reader feedback.
  - Backlog hygiene pass over the nine open issues (#135's September bet 1) - promoted 2026-09-06, still not started at two elapsed weeks. No open issue was touched in the window; the most recent update among the nine is #37 at 2026-05-29.

- **Promoted (Next → Now):**
  - None.

- **Other Moves:**
  - PR #112 - Blocked → Now; recorded last week as soft-blocked awaiting reader feedback, now actively revised and marked ready for review without it.
  - PR #114 - Blocked → Now; same, resumed unprompted and marked ready for review.

## Metrics Snapshot

- **PRs merged:** 1 (#140 prior heartbeat)
- **Issues closed:** 0
- **Current WIP:** 3/2
- **Build status:** ✅ Green (CI passed on every run in the window, including #112's head `ca27390` and #114's head `6f02929` on 09-19; no failures)

## Notable Outcomes

- The drafts came back to life. #112 took five content commits across 09-18 and 09-19 — collapsible territory cards, a completed four-step Fundamental Algorithm, the car analogy retired in favor of the surfing metaphor, a Part IV worksheet extracted into a companion file, a "Priority Trap" explanation, and four diagrams pulled out into standalone SVG assets. #114 took one: tightened essay flow, cut redundant case studies, real service reordered ahead of performance service. Both modules were reset to `v1.0.0` as inaugural releases and marked ready for review (09-18 and 09-19). This is the first content work on either branch since 2026-06-03/06-04, and it happened without the reader feedback both PRs were nominally waiting on — which is the honest read of that soft blocker: it was never going to arrive, and the work was unblocked all along.

- Now exceeds its ADR 0001 cap at 3/2 (the first time Now has been at or above cap since 2026-01-13). Both #112 and #114 count as distinct items under the #138 rules because each was actively revised and marked ready for review, rather than grouped under a single pass or listed beside the count as "in flight." Alongside the unstarted backlog hygiene pass carried over from 2026-09-06, Now is over-committed. The consequence is the one ADR 0001 intends the cap to force: the backlog hygiene pass is sharing the column with active content work, having produced nothing in the two weeks it has held a slot. Last week's file set the demotion test at a second full week untouched. That test is now met; demoting the hygiene pass out of Now would restore WIP to the 2/2 cap.

- Worth a look before these PRs merge: commit `ca27390` on #112 carries repo-wide infrastructure — `scripts/export.sh` and `docs/policies/pandoc.yaml` gain a module resource-path, CI gains `librsvg2-bin`, `Base.astro` gains responsive image styling — plus compiled PDF and EPUB printables committed as files. Export-pipeline changes and build artifacts riding inside a content PR means neither lands until the PR does, and the printables are generated output under version control. Splitting the infra out would let it merge on its own schedule. No product merge again this week, putting that streak at fifteen consecutive weeks; #140 is meta and counts in Metrics but not in the streak. #52's re-evaluation trigger (first synthesis after a week with any non-meta merge) remains unfired, though a #112 or #114 merge would fire it.

## Blockers & Needs

- No hard blockers.
- The routine prompt and the repo template are still drifted, unchanged from last week. The prompt's format block carries the three consistency rules but not the **Other Moves** subsection `heartbeat-template.md` has required since #138, so each week's file starts from a stale skeleton — this one included, with the subsection added by hand.
- The "awaiting reader feedback" framing on #112/#114 is now spent. Three months of it produced no feedback; the work moved when the author moved it, and both are now marked ready for review. Either name a reader or drop the framing.

## Next Week Focus

- Decide the hygiene pass: start it against #17, #18, #22, #24, #28, #36, #37, #52, #87, or demote it out of Now to bring WIP back to the 2/2 cap. Two weeks unstarted meets the test last week's file set, and with both content PRs (#112 and #114) occupying slots in Now, the cost of holding it is no longer hypothetical. Same caveats: #52 is parked with an unfired trigger, so hygiene means labeling it accurately rather than reopening its sizing question, and #17 is already titled "(parked)".
- Carry #112 and #114 toward mergeable. Both are at `v1.0.0` with green CI and marked ready for review; the open questions are the accessibility checklist (`docs/policies/ACCESSIBILITY_CHECKLIST.md`, required before publishing), whether the export/CI changes in `ca27390` should split into their own PR, and whether committed printables belong in the repo.

---
Heartbeat duration: ~10 minutes
