# Weekly Heartbeat — Week of 2026-06-08

## Board Movement

- **Completed (Now → Done):**
  - #110 - own-resonance highlight no longer waits for a reload; optimistic update + "You resonated" tooltip wording, with `localStorage` persistence (PR #111, merged 2026-06-04). Direct response to pilot feedback.
  - PR #113 - dropped the now-unused `inserted` field from `record-resonance` after #111 made the client's count independent of new-write vs. dedup (merged 2026-06-04).
  - PR #115 - production On Balance logo and iOS `apple-touch-icon.png` in the site (merged 2026-06-05).
  - PR #116 - rounded out the icon/social set: 1200×630 `og:image` social card, PWA `manifest.webmanifest` with maskable icons, and multi-resolution `favicon.ico` fallback (merged 2026-06-05).

- **In Progress (Now):**
  - #52 - Phase 1 synthesis retrospective. No visible progress this week; capacity went to site polish and the alignment-system draft.
  - PR #112 - draft *The Alignment System* module opened for early review on a Netlify Deploy Preview (per-axis visuals, navigation diagram, recolored Four Territories map). Direct execution on the May-synthesis carry-forward bet.
  - PR #114 - draft *The Case for Self-Work as Service* module opened the same way; third and final essay in the series, faithful conversion from the Google Doc into house markdown style.

- **Promoted (Next → Now):**
  - None this week. #52 remains the headline issue; #112 and #114 are bonus draft work surfaced for reader feedback rather than promotions off the backlog.

## Metrics Snapshot

- **PRs merged:** 5 (#109 prior heartbeat, #111, #113, #115, #116)
- **Issues closed:** 1 (#110)
- **Current WIP:** 1/2 (#52); plus draft PRs #112 and #114 in flight
- **Build status:** ✅ Green

## Notable Outcomes

- The May-synthesis carry-forward — "commit or formally retire The Alignment System draft" — landed on the commit side: PR #112 puts a reviewable cut in front of readers via Deploy Preview rather than continuing to drift in private. A second draft module (#114, *The Case for Self-Work as Service*) opened the same way, ahead of plan.
- Pilot feedback is still paying off after #51 closed: response #3 on the resonance feedback form ("highlights didn't show up") was a missing-feature bug, not a storage failure. #111 fixes it via an optimistic client-side update with `localStorage`-keyed persistence; #113 is the small dead-signal cleanup that followed. A known limitation remains — `record-resonance` does not dedupe by `user_fingerprint`, so "+N others" math is exact only when the same passage isn't resonated twice; flagged for a possible follow-up.
- Visual identity for the site is now in place across surfaces (`favicon.svg`, `apple-touch-icon`, `og:image`, PWA manifest, `.ico` fallback). #52 took no time this week; the alignment-system draft and site polish absorbed the capacity instead.

## Blockers & Needs

- No hard blockers. #52 continues to be capacity-gated rather than dependency-gated.

## Next Week Focus

- Begin #52 in earnest — pull the Google Form responses and production `data/resonance` data together and start the Phase 1 retrospective draft. Sized in the May synthesis to run through June, but it has not moved in two weeks.
- Cycle reader feedback on PR #112 (alignment system) and PR #114 (self-work as service) toward a merge decision; consider opening a follow-up for server-side fingerprint dedupe in `record-resonance`.

---
Heartbeat duration: ~10 minutes
