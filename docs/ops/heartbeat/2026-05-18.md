# Weekly Heartbeat — Week of 2026-05-18

## Board Movement

- **Completed (Now → Done):**
  - #99 - v1.0 landing page, OG / Twitter card metadata, canonical site URL, and a hand-authored cairn SVG favicon (PR #99)
  - #100 - v1.0.0 release plumbing: root and `site/` `package.json` bumps to `1.0.0`, lockfile updates, CHANGELOG `v1.0.0` entry, and `MD024 siblings_only` config so reused release section headings stop tripping the linter (PR #100)
  - #101 - README clarifies root vs `site/` as separate npm packages and adds an explicit Site getting-started block (PR #101)
  - #102 - `[dev]` block in `netlify.toml` so `npx netlify dev` from the repo root proxies Astro and `/.netlify/functions/*` on `:8888` instead of serving an empty `site/dist/`; README gains a "Local dev with Netlify Functions" section and a fine-grained `GITHUB_TOKEN` walkthrough (PR #102)
  - #103 - Split resonance failures into retriable (network/abort, 429, 5xx) vs terminal (400, 403, 404, 503) via a new `ResonanceError` class; terminal errors now show "Couldn't save" and stay until dismissed instead of pretending a retry might help. Read/write paths share a `logResonanceFailure` helper that hints at `:8888` vs `:4321` on 404s (PR #103)

- **In Progress (Now):**
  - #97 - v1.0.0 release umbrella: site, version, and Windows-verification tasks are all checked. Only the production `data/resonance` wipe and the final post-wipe smoke test remain blocking, plus two non-blocking cleanups (#95 diagnostic `console.log`, #78 shared origin validation).

- **Promoted (Next → Now):**
  - None — #97 is still the active focus and is close to closing.

## Metrics Snapshot

- **PRs merged:** 5 (#99, #100, #101, #102, #103)
- **Issues closed:** 0
- **Current WIP:** 1/2
- **Build status:** ✅ Green

## Notable Outcomes

- v1.0.0 release prep landed in two coordinated PRs: #99 (site-facing — landing page, share metadata, favicon, canonical URL) and #100 (plumbing — version bumps to `1.0.0` across both packages and the `v1.0.0` CHANGELOG entry). #97's checklist is now down to the production data wipe and the post-wipe smoke test; everything else is done.
- Contributor onboarding got a meaningful pass. A fresh-clone test of #100 surfaced that `site/node_modules` is its own install (fix in #101), and #102 fixed `npx netlify dev` falling back to the empty `site/dist/` — both classes of failure a new contributor would hit on day one. The README now walks through `:4321` vs `:8888`, the `data/resonance-staging` routing, and a step-by-step `GITHUB_TOKEN` setup.
- #103 cleaned up a long-standing UX papercut in `ResonancePopup`: previously every failure showed "Error — try again" and auto-reset, even when retrying was pointless (e.g. 403, 404). The new `ResonanceError` carries `status`, server `code`, `retryAfter`, and a `retriable` flag, and the popup branches on it. Trade-off noted in the PR: the terminal state is sticky until the user dismisses, which is the intended behavior but a change from the previous self-clearing pattern.

## Blockers & Needs

- Production `data/resonance` wipe is the last hard blocker on v1.0.0 launch — destructive op, parked to be done close to actual launch.
- Diagnostic `console.log` from #95 still wants removal once a few clean production runs are visible in function logs.

## Next Week Focus

- Close #97: do the production `data/resonance` wipe and the final smoke test, then cut v1.0.0.
- If there's slack after launch, knock out the two cleanup follow-ups on #97 (#95 `console.log` removal, #78 shared origin validation).

---
Heartbeat duration: ~10 minutes
